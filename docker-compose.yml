version: '3'
services:
  mongodb:
    image: mongo:latest
    ports:
      - "27018:27017" 
    volumes:
      - mongo_data:/data/db 
  tracker:
    build: 
      context: tracker
    depends_on:
      - mongodb
volumes:
  mongo_data: